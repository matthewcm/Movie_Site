<div><h1></h1></div>

<div><li><h4>Search term was : <%= @series_name.gsub('+',' ') %></h4></li></div>
<div><h1></h1></div>

<% results = series_search(@series_name.gsub('+',' '), params[:sort_key] , 10)%>
<% if results != false%>
    <% results.each do |r| %>
    <% r = series_search(r['imdbID']) %>
    <div class = 'col-sm-6 col-lg-4'><h4><%= link_to("#{r["Title"]} ( #{r["Year"]} )".truncate(30), "/series/"+(r["imdbID"]).to_s)%></h4>
    <div style="height: 400px;"><%=  link_to(image_tag(single_poster(r), :alt => "#{r['Title']} tv-series poster", :width => "200", :class =>"thumbnail"),  "/series/"+(r["imdbID"]).to_s) %> </div>
    </div>
    <% end %>
<% else %>

<p>No series exist with that name... Sorry </p>
<p>Oh well.. At least you get to look at these cute pandas</p>

   <div> <%= image_tag("nothing_here.gif", :class => 'img-rounded img-thumbnail') %></div>
<% end  %>